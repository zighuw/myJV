# myJV 合成器 Vibe Coding 开发计划

| 项目 | 内容 |
| --- | --- |
| 项目代号 | **myJV**（JV-1080 Patch 参数体系 + 自定义采样器） |
| 文档版本 | v2.0（Vibe Coding 版，替代 v1.1 传统计划） |
| 关联文档 | `myJV_开发架构文档.md`（v1.4，技术基线） |
| 开发模式 | AI 编码代理为主实现；人类负责规格、审查与验收 |
| 计划基线 | 26 周（瓶颈为审查与调音，而非编码） |
| 编制日期 | 2026-09-19 |

### v2.0 改写说明

1. **工作流改写**：按 Vibe Coding 模式重组为"任务卡 → 提示词 → AI 实现 → 自动门禁 → 人类审查 → 合并"的循环。
2. **审查协议（核心新增）**：新增 R0–R4 五级审查、AI 常见缺陷红旗清单、测试质量审查、交叉审查、证据要求。
3. **任务表调整**：保留 M0–M6 里程碑与任务 ID（便于与架构文档追溯）；交付物列改写为"证据"；新增"审查级别"与"AI 会话"列。
4. **新增章节**：上下文工程与会话管理、记忆文件（AGENTS.md 等）、范围纪律、依赖政策。
5. **保留内容**：里程碑时间窗、验收标准、风险与应急裁剪、后续版本路线图。

---

## 目录

1. [概述](#1-概述)
2. [角色与协作循环](#2-角色与协作循环)
3. [代码审查协议（核心）](#3-代码审查协议核心)
4. [任务体系](#4-任务体系)
5. [上下文工程与会话管理](#5-上下文工程与会话管理)
6. [质量保障](#6-质量保障)
7. [纪律与护栏](#7-纪律与护栏)
8. [进度与节奏](#8-进度与节奏)
9. [风险与对策](#9-风险与对策)
10. [验收标准](#10-验收标准)
11. [后续版本路线图](#11-后续版本路线图)
12. [附录 A：任务卡模板](#附录-a任务卡模板)
13. [附录 B：任务提示词模板](#附录-b任务提示词模板)
14. [附录 C：审查清单](#附录-c审查清单)
15. [附录 D：会话交接模板](#附录-d会话交接模板)
16. [附录 E：参考资料](#附录-e参考资料)

---

## 1. 概述

### 1.1 开发模式定义

本项目采用 **Vibe Coding** 模式：

- **AI 编码代理**（如 opencode / Claude Code 等）承担绝大部分代码实现、测试编写与文档草拟；
- **人类**负责需求与规格、任务拆分、上下文投喂、代码审查、证据核验与最终验收；
- 代码质量由**自动化门禁 + 分级人工审查**共同保证，不依赖对 AI 输出的信任。

### 1.2 核心原则

| 原则 | 说明 |
| --- | --- |
| 规格先行 | 先冻结规格（架构文档/任务卡），再让 AI 实现；禁止"边写边定需求" |
| 小步快跑 | 每个任务卡 1 个 PR、1 个 AI 会话、≤ 400 行变更（可读性阈值） |
| 测试即事实 | 行为正确性以测试、基准音频、sanitizer 输出为准，不以 AI 自述为准 |
| 证据驱动 | 没有证据（测试输出/数据/截图）的任务不算完成 |
| 人类守门 | 关键模块必须逐行审查；AI 不得自行合并、不得修改门禁、不得改测试来"过" |
| 可追溯 | 提交信息带任务 ID；审查记录与提示词留档，可回溯每个变更的来源与理由 |

### 1.3 文档关系

```
架构文档 v1.4 ── 技术规格与约束（冻结基线）
       │
       ▼
本计划 v2.0 ─── 任务卡、审查协议、会话流程
       │
       ▼
AGENTS.md / PROGRESS.md / DECISIONS.md ── 每个会话的上下文与状态
```

---

## 2. 角色与协作循环

### 2.1 角色

| 角色 | 承担者 | 职责 |
| --- | --- | --- |
| 产品/架构负责人 | 人类 | 冻结规格、拆分任务卡、裁决争议、里程碑验收 |
| 审查者 | 人类 | 按 R1–R4 级别审查代码与证据；维护红旗清单 |
| 实现者 | AI 代理 | 按任务卡实现、写测试、产出证据、提交 feature 分支 |
| 交叉审查者 | 第二 AI 会话（可选第二人） | 以"找茬"模式审查关键 diff，输出问题清单 |
| 审计者 | 人类（周期性） | 每 2 周做一次全库质量扫描（见 6.4） |

### 2.2 协作循环

```
① 人类：从 Backlog 取任务卡，补全上下文（规格章节/接口/禁止事项）
        ↓
② AI：一个会话只做一个任务卡 → 实现 + 测试 + 证据 → feature 分支提交
        ↓
③ 自动门禁 R0：构建 / 单元测试 / sanitizer / pluginval / 基准音频
        ↓（全绿才进入人工审查）
④ 人类：按级别审查（R1/R2/R3）→ 问题清单 → AI 修复（新提交）
        ↓
⑤ 人类：合并到 develop，更新 PROGRESS.md；R4 里程碑评审
```

### 2.3 决策与升级

- AI 遇到规格歧义 → **停止实现**，在任务卡中提出问题清单，由人类裁决后继续；
- AI 需要修改受保护区域（见 4.3）→ 必须先在任务卡中申请，人类批准后方可修改；
- AI 发现架构缺陷 → 提交 ADR 草案，不得擅自"顺手修复"。

---

## 3. 代码审查协议（核心）

> 本章是本计划最重要的部分。Vibe Coding 的风险集中在"看起来对但实际错"的代码上，
> 审查必须比传统开发更结构化、更依赖证据。

### 3.1 审查分级

| 级别 | 名称 | 执行者 | 适用 | 时长参考 |
| --- | --- | --- | --- | --- |
| **R0** | 自动门禁 | CI | 所有变更 | — |
| **R1** | 常规审查 | 人类 | 常规任务（UI、参数接线、文档） | 10–20 min |
| **R2** | 深度审查 | 人类 | 关键模块（RT 路径、DSP、资产、序列化） | 30–60 min |
| **R3** | 交叉审查 | 第二 AI 会话 / 第二人 | R2 模块与里程碑收口 | 30–60 min |
| **R4** | 里程碑评审 | 人类 | 每个里程碑结束 | 1–2 h |

**R0 门禁清单（全绿才可进入人工审查）**：

1. 三平台构建通过；
2. 全部单元/集成测试通过，**无跳过/禁用用例**；
3. ASan/UBSan 无新增问题；RTsan 无违规；
4. 基准音频回归通过（或差异已附说明）；
5. `pluginval` level 5+ 通过（Windows/macOS）；
6. 变更范围与任务卡一致（无任务外文件改动，受保护区域除外）。

**R2 触发条件（满足其一即升级）**：

- 涉及音频线程路径（`// RT-safe` 标注文件）；
- 涉及资产发布/回收、线程同步、内存所有权；
- 涉及 DSP 数学（滤波、包络、LFO、采样插值、FXM/Booster）；
- 涉及序列化/迁移、参数 ID 变更、`Calibration.h` 常量；
- 涉及构建系统、CI、依赖变更。

### 3.2 审查流程

1. AI 在 `feature/<任务ID>-<简述>` 分支提交（小提交，信息带任务 ID）；
2. AI 产出**证据包**（见 6.1），贴在 PR 描述中；
3. CI 执行 R0；失败则退回 AI 修复（不得通过修改测试/门禁绕过）；
4. 人类按级别审查：先看证据，再看 diff，最后抽查测试；
5. 审查意见以"问题清单"形式返回（严重级/文件:行/理由/期望）；
6. AI 逐条修复（**新提交，不 amend、不强推**）；
7. 复审通过后合并；更新 `PROGRESS.md`。

**PR 大小纪律**：单 PR 变更 ≤ 400 行（不含测试与生成文件）；超限则拆分为多个任务卡。UI 换肤、批量重命名等机械变更可例外，但需单独 PR。

### 3.3 红旗清单（AI 常见缺陷）

> 审查时优先扫描以下模式；发现任意一项即要求修复或说明。

**实时安全（最高优先级）**

- [ ] RT 路径出现 `new/delete`、`std::vector` 扩容、`std::string` 构造；
- [ ] RT 路径出现 `shared_ptr` 拷贝/析构、锁、日志、文件 IO、异常；
- [ ] 在 RT 路径读取 UI/模型层可变状态（未走快照）；
- [ ] 异步回调捕获 `this` 或裸引用，生命周期不明。

**内存与并发**

- [ ] 返回局部对象引用/指针；悬空 `const ZoneSet*`；
- [ ] 未初始化成员；数组越界（尤其采样插值与循环点边界）；
- [ ] 数据竞争（TSan 未覆盖的新路径）；
- [ ] `std::atomic` 内存序使用错误或缺失。

**数值与 DSP**

- [ ] 除零、NaN/Inf 未防护；denormal 未处理；
- [ ] 硬编码 magic number（应进 `Calibration.h`）；
- [ ] 参数映射曲线端点/单调性错误；
- [ ] 包络 Release 未从当前值出发；重触发跳变；
- [ ] 采样循环交叉淡化边界错误（loopStart == loopEnd 等）。

**语义与规格**

- [ ] 参数范围/默认值与架构文档不一致；
- [ ] 调制作用点错误（音高/截止/电平/声像顺序）；
- [ ] 结构路由与架构文档 5.10 不符；
- [ ] 注释与代码行为不符（AI 常见"自信的错误注释"）。

**测试与工程**

- [ ] 测试断言过弱/恒真；只测实现细节；无边界用例；
- [ ] 测试被修改以"适应"实现（未经批准）；
- [ ] 悄悄重构任务外代码；引入未批准依赖；
- [ ] 复制来源不明的代码（许可风险）。

### 3.4 关键模块深度审查清单（R2）

**资产与生命周期（`AssetReclaimer` / `PatchRuntime`）**

- [ ] 发布/退休路径无锁、无引用计数；活动 Runtime 不可回收；
- [ ] `oldestAssetInUse` 计算覆盖无 Note 情形；
- [ ] 宽限期与压力测试（频繁发布/回收）通过 ASan。

**ToneVoice 与渲染（RT 路径）**

- [ ] 分阶段接口的调用顺序符合 5.3/5.10；
- [ ] `updateModulators` 每采样恰好一次；
- [ ] KillFading 状态转换正确；`finished()` 语义明确；
- [ ] 无每采样分支抖动（类型/开关在块首决策）。

**采样器（`SamplePlayer`）**

- [ ] 插值越界防护（首尾样本、反向、极短采样）；
- [ ] 循环/交叉淡化在采样率与音高比极端值下正确；
- [ ] 采样率比 `sample.sr / engine.sr` 参与音高计算；
- [ ] Note-On 淡入、Voice 结束淡出无爆音。

**序列化（`PatchSerializer`）**

- [ ] `schemaVersion` 迁移函数表完整；未知字段容错；
- [ ] 浮点往返精度（0–127 参数）无损；
- [ ] 缺失采样/哈希不匹配时的降级路径。

### 3.5 测试质量审查

AI 写的测试必须经过质量审查（可由人类抽查 + R3 交叉审查）：

1. **有效性**：手工破坏一处实现（变异抽查），测试必须变红；不变红则测试无效；
2. **边界**：端点值、空/单元素、极值、非法输入；
3. **确定性**：随机源使用固定种子；测试可重复；
4. **独立性**：不依赖执行顺序与全局状态；
5. **断言强度**：断言行为结果而非内部实现；禁止 `EXPECT_TRUE(true)` 类空断言；
6. **禁止绕过**：不得 `skip`/`disable` 用例；不得删除失败用例；不得放宽容差以通过。

> 审查记录中标注"测试已做变异抽查"（抽查的用例与结论）。

### 3.6 交叉审查（R3）

- 使用**新会话**（建议不同模型或重置上下文）执行"审查者"提示词；
- 提示词要点：只找问题、不重写代码；对照架构文档章节；重点检查 RT 安全、边界、测试弱点、与规格不符；
- 输出：问题清单（严重级：S1/S2/S3 + 文件:行 + 理由）；
- 人类裁决后转成修复任务或记录为已知问题；
- R3 不替代人类审查，只作为补充信号。

### 3.7 审查记录与追溯

- PR 模板必填：任务 ID、变更摘要、证据包链接、审查级别、审查人、遗留问题；
- 提交信息格式：`<type>(<任务ID>): <摘要>`（如 `feat(M2-04): add TPT SVF with control-rate coefficients`）；
- `DECISIONS.md` 记录审查中形成的架构决策与例外批准；
- 每个里程碑归档：任务卡、证据包、审查记录（便于复盘与交接）。

---

## 4. 任务体系

### 4.1 任务卡格式

每个任务在 Backlog 中以任务卡存在，字段如下（模板见附录 A）：

| 字段 | 说明 |
| --- | --- |
| ID | `M<里程碑>-<序号>`（沿用既有编号） |
| 目标 | 一句话可验证的目标 |
| 上下文 | 架构文档章节号、相关头文件、参数 ID 清单 |
| 交付物 | 源文件/测试/文档清单 |
| 证据 | 必须附带的证据（测试输出/sanitizer/基准/截图） |
| 验收标准 | 可客观判定的完成条件（DoD） |
| 禁止事项 | 本任务不允许的改动（如不得动受保护文件） |
| 审查级别 | R1 / R2（R3 在里程碑收口统一执行） |
| 依赖 | 前置任务 ID |
| 预估 | AI 会话数 |

### 4.2 粒度与拆分规则

- 一个任务卡 = 一个 PR = 一个 AI 会话（允许同会话内修复审查意见）；
- 若预估会话 > 3，或涉及两个以上模块，必须拆分；
- 若任务无法写出客观验收标准，说明规格不清，先补规格再拆任务；
- 优先按"可独立验证的行为"拆分（如"包络 Release 从当前值出发"独立于"包络时间映射"）。

### 4.3 受保护区域

以下文件/区域**仅允许在人类明确批准的任务卡中修改**，且审查级别强制 R2（必要时 R3）：

| 受保护区域 | 原因 |
| --- | --- |
| `Source/Engine/AssetReclaimer.*`、`PatchRuntime` 定义 | 生命周期与并发正确性 |
| `Source/Engine/ToneVoice.*` 的 RT 路径 | 实时安全与 DSP 正确性 |
| `Source/Engine/VoiceManager.*` | 声部分配与抢占 |
| `Source/Params/Calibration.h` | 全局映射常量 |
| `Source/IO/PatchSerializer.*` | 数据兼容性 |
| `CMakeLists.txt`、`.github/`、CI 配置 | 门禁完整性 |
| 依赖清单（新增第三方库） | 许可与体积 |
| `myJV_开发架构文档.md` | 技术基线 |

### 4.4 状态流与看板

```
Backlog ─▶ Ready（上下文已补全）─▶ In Progress（AI 会话中）
        ─▶ In Review（R0 绿，等待人工）─▶ Changes Requested ─▶ In Progress
        ─▶ Done（合并 + PROGRESS 更新）         Blocked（规格歧义/依赖）
```

- 看板可用仓库 Issues/Projects 或 `PROGRESS.md` 表格维护；
- "Done" 的判定以合并 + 证据归档为准，不以 AI 自述为准。

### 4.5 里程碑与任务批次

**里程碑总览**

| 里程碑 | 目标 | 任务数 | AI 会话（估） | 人类审查（估） | 时间窗 |
| --- | --- | --- | --- | --- | --- |
| M0 | 工程骨架：可加载插件、MIDI 通路、测试音、CI | 7 | 10 | ~9 h | W1–W2 |
| M1 | 采样器子系统 + 资产回收 | 9 | 18 | ~11 h | W3–W6 |
| M2 | 单 Tone 引擎（WG/TVF/TVA/ENV/LFO/矩阵） | 11 | 25 | ~17 h | W7–W11 |
| M3 | 完整 Patch（4 Tone/Structure/声部/序列化） | 8 | 17 | ~13 h | W12–W15 |
| M4 | 演奏表现力（Mono/Portamento/Patch Remain 等） | 9 | 14 | ~9 h | W16–W18 |
| M5 | UI 完善（控件/可视化/撤销/预设） | 8 | 17 | ~8 h | W19–W22 |
| M6 | 调音、优化、兼容与发布 | 8 | 18 | ~12 h | W23–W26 |
| 合计 | — | 60 | **~119** | **~79 h** | 26 周 |

> 会话定义：一次聚焦的 AI 工作会话（实现 + 自测 + 证据），不含修复迭代；修复迭代通常在同一会话内完成。
> 人类审查时长不含 M6 调音的主观听感迭代（该部分按实际节奏安排）。
> 瓶颈说明：AI 加速编码，但审查/验证/调音由人类完成，故总工期仍以 26 周为基线（审查吞吐高时可压缩至 20–22 周）。

**M0 工程骨架**

| ID | AI 任务（目标） | 交付物与证据 | 审查 | 依赖 | 会话 |
| --- | --- | --- | --- | --- | --- |
| M0-01 | CMake + JUCE 工程（三平台，VST3/AU/Standalone） | 三平台构建日志；宿主加载截图 | R2 | — | 2 |
| M0-02 | 目录与模块骨架（按架构 9.2） | 空模块编译通过；接口头文件清单 | R1 | M0-01 | 1 |
| M0-03 | APVTS 参数框架 + `ParameterIDs` 生成 | 参数注册表；示例参数自动化演示 | R1 | M0-01 | 2 |
| M0-04 | 引擎空壳 + `BusBuffers` + 测试音 | 总线布局验证；测试音 WAV | R2 | M0-01 | 1 |
| M0-05 | MIDI 子块切分框架 | 采样精度分发单元测试输出 | R2 | M0-04 | 1 |
| M0-06 | CI 流水线（构建/测试/sanitizer/pluginval） | CI 全绿截图；PR 模板 | R2 | M0-01 | 2 |
| M0-07 | 日志/断言/崩溃报告基础设施 | 配置说明；RT 路径无日志的证据 | R1 | M0-01 | 1 |

**M1 采样器子系统**

| ID | AI 任务（目标） | 交付物与证据 | 审查 | 依赖 | 会话 |
| --- | --- | --- | --- | --- | --- |
| M1-01 | `Sample`/`Zone`/`ZoneSet` 数据模型 | 结构对照架构附录 B；单元测试输出 | R1 | M0-02 | 1 |
| M1-02 | `SampleLibrary`：后台扫描、哈希去重、索引 | 扫描/去重/缺失检测测试输出 | R2 | M1-01 | 2 |
| M1-03 | `SampleImporter`：解码、循环点、重采样、缩略图 | 各类文件导入测试；循环点比对 | R2 | M1-01 | 2 |
| M1-04 | `SamplePlayer`：音高比、Hermite、循环/淡化、反向 | 单元测试 + 听感样本 WAV | R2 | M1-03 | 3 |
| M1-05 | Zone 选择策略（Note-On 一次选定） | 并列/无命中用例测试输出 | R1 | M1-01 | 1 |
| M1-06 | `PatchRuntime` 与 `AssetReclaimer`（发布/退休/宽限期） | ASan 压力测试日志；R3 问题清单 | R2+R3 | M1-01 | 3 |
| M1-07 | 采样库 UI：浏览器、导入、基础试听 | 操作截图；UI 不阻塞音频的证据 | R1 | M1-02/04 | 2 |
| M1-08 | Zone 编辑器基础：网格、属性面板、自动映射 | 操作截图；映射正确性测试 | R1 | M1-05 | 2 |
| M1-09 | 波形视图：缩略图、循环点/交叉淡化拖拽 | 操作截图；编辑即时生效验证 | R1 | M1-03/08 | 2 |

**M2 单 Tone 引擎**

| ID | AI 任务（目标） | 交付物与证据 | 审查 | 依赖 | 会话 |
| --- | --- | --- | --- | --- | --- |
| M2-01 | `ParamSnapshot` 构建与每块刷新（活动 Runtime） | 热更新无爆音测试；快照隔离测试 | R2 | M1-06 | 2 |
| M2-02 | `Envelope`：P/F/A-ENV、力度/键跟随、Release 从当前值 | 时长/形状/重触发测试输出 | R2 | M2-01 | 3 |
| M2-03 | `LFO`：8 波形、Delay/Fade、同步、确定性种子 | 波形/同步/淡入测试输出 | R2 | M2-01 | 2 |
| M2-04 | `SVF`/`Biquad`：5 类型、cutoff/Q 映射、控制率系数 | 频响对比数据（±0.5 dB）；稳定性扫描 | R2 | M2-01 | 2 |
| M2-05 | `ToneVoice` 分阶段接口与单 Tone 渲染循环 | 调制顺序核对表；R3 问题清单 | R2+R3 | M2-02/03/04 | 2 |
| M2-06 | WG 接线：Wave Gain、FXM、Tone Delay、键区/力度、开关 | 各参数可测证据；Tone Delay 模式测试 | R2 | M2-05 | 3 |
| M2-07 | Control 矩阵（3×4）与 Hold/Peak | 作用点核对表；矩阵测试输出 | R2 | M2-05 | 2 |
| M2-08 | 参数平滑（Level/Pan/Cutoff/Resonance） | 突变扫描测试（无 zipper） | R1 | M2-05 | 2 |
| M2-09 | 单 Tone 编辑页面（标准控件，功能优先） | 页面截图；参数可达清单 | R1 | M0-03/05 | 3 |
| M2-10 | 基准音频框架（确定性渲染/基线/容差） | CI 基准作业运行日志 | R2 | M2-05 | 2 |
| M2-11 | 映射常量首轮调音 | 调音记录表；基准更新说明 | R2 | M2-10 | 2 |

**M3 完整 Patch 与声部管理**

| ID | AI 任务（目标） | 交付物与证据 | 审查 | 依赖 | 会话 |
| --- | --- | --- | --- | --- | --- |
| M3-01 | `VoiceManager`：64 声部、Note 池、KillFading、抢占 | 分配/抢占/满载测试；R3 问题清单 | R2+R3 | M2-05 | 3 |
| M3-02 | `Note` + `TonePairProcessor`：Structure 1–4、Booster、RingMod | 四结构基准音频；R3 问题清单 | R2+R3 | M3-01 | 3 |
| M3-03 | Patch Common 接线与页面 | 参数生效测试；页面截图 | R1 | M3-01 | 2 |
| M3-04 | Tone 2–4 实例与页面复用 | 无串扰测试；页面截图 | R1 | M3-01/02 | 1 |
| M3-05 | 补丁序列化（`.myjvp`、schemaVersion、迁移） | 往返一致性测试；迁移用例 | R2 | M2-01 | 2 |
| M3-06 | 宿主状态存取（异步加载、占位 Zone） | 宿主保存/恢复录屏；加载不阻塞证据 | R2 | M3-05 | 2 |
| M3-07 | 预设管理（默认补丁、保存/加载/浏览） | 操作截图；默认补丁清单 | R1 | M3-05 | 2 |
| M3-08 | 参数覆盖扫描与结构基准测试 | 覆盖报告（无遗漏）；基准通过日志 | R2 | M3-02/05 | 2 |

**M4 演奏表现力**

| ID | AI 任务（目标） | 交付物与证据 | 审查 | 依赖 | 会话 |
| --- | --- | --- | --- | --- | --- |
| M4-01 | Mono/Poly 与 Legato（复用 ToneVoice） | 行为测试输出 | R1 | M3-01 | 2 |
| M4-02 | Portamento 状态机（时间/速率、起始方式） | 听感样本；状态机测试 | R1 | M4-01 | 2 |
| M4-03 | Hold-1 延音、All Notes Off / All Sound Off | MIDI 行为测试输出 | R1 | M3-01 | 1 |
| M4-04 | Patch Remain 完善（Runtime 切换、冻结快照） | 频繁切换压力测试；R3 问题清单 | R2+R3 | M3-06 | 2 |
| M4-05 | Analog Feel、Stretch Tune、Random Pitch、Random/Alt Pan | 确定性种子复现测试 | R1 | M3-03 | 2 |
| M4-06 | Tone Delay 三模式完善（含 Note-Off 取消） | 模式行为测试输出 | R2 | M2-06 | 2 |
| M4-07 | FXM 与 Bias 完善 | 频谱/波形对比图 | R1 | M2-06 | 1 |
| M4-08 | 参数语义规格定稿（Tone Delay/Control/Audition） | 规格文档更新；架构 13.1 关闭记录 | R1 | M1-07 | 1 |
| M4-09 | MIDI 完善（Bank Select/PC、CC、Aftertouch、RPN） | MIDI 实现核对表 | R1 | M0-05 | 1 |

**M5 用户界面完善**

| ID | AI 任务（目标） | 交付物与证据 | 审查 | 依赖 | 会话 |
| --- | --- | --- | --- | --- | --- |
| M5-01 | 主题/LookAndFeel + 顶部状态栏 | 视觉截图；DPI 缩放验证 | R1 | M0-03 | 2 |
| M5-02 | 控件库完善（JvKnob/JvSlider/JvLcd 等） | 交互截图；双击复位测试 | R1 | M5-01 | 3 |
| M5-03 | 可视化控件（Envelope/FilterCurve/LfoScope/ToneLevelMeter） | 联动录屏 | R1 | M5-01 | 3 |
| M5-04 | Sampler 页面完善（拖拽、自动映射、波形编辑整合） | 完整工作流录屏 | R1 | M1-08/09 | 3 |
| M5-05 | 键盘页与试听联动 | 交互录屏 | R1 | M1-07 | 1 |
| M5-06 | 撤销/重做（参数级 + 采样编辑） | 撤销栈测试；无泄漏证据 | R2 | M3-05 | 2 |
| M5-07 | 参数显示与映射一致（单一来源） | 显示核对表 | R1 | M2-11 | 1 |
| M5-08 | UI 性能优化（懒加载/复用/缓存） | 页面切换耗时；空闲 CPU 数据 | R2 | M5-02 | 2 |

**M6 调音、优化与发布**

| ID | AI 任务（目标） | 交付物与证据 | 审查 | 依赖 | 会话 |
| --- | --- | --- | --- | --- | --- |
| M6-01 | 映射常量逐项调音 | 调音记录表；基准更新；R3 问题清单 | R2+R3 | M2-11 | 4 |
| M6-02 | 基准音频重生成与评审 | 差异清单；人工确认记录 | R2 | M6-01 | 2 |
| M6-03 | 性能优化（控制率/SIMD/快速路径） | 前后性能对比（64 声部 < 30%） | R2 | M3-01 | 3 |
| M6-04 | 稳定性（ASan/RTsan 长跑、72h 压力） | 长跑日志；无崩溃/泄漏结论 | R2 | M5-08 | 2 |
| M6-05 | 兼容性矩阵（宿主 × 采样率 × 块大小） | 矩阵测试报告 | R2 | M5-08 | 2 |
| M6-06 | 打包与签名（Win 安装包/macOS 公证） | 安装包；签名验证截图 | R2 | M6-04 | 2 |
| M6-07 | 文档（用户手册/快速上手/发行说明） | 文档评审记录 | R1 | M5-08 | 2 |
| M6-08 | 发布检查单与 RC 验收 | 检查单全项记录；tag | R4 | M6-01..07 | 1 |

---

## 5. 上下文工程与会话管理

### 5.1 上下文注入清单

每个 AI 会话开始时，必须注入**最小充分上下文**：

| 类别 | 内容 |
| --- | --- |
| 规格 | 架构文档中与任务相关的章节（给出章节号，不整篇投喂） |
| 接口 | 相关头文件与已冻结的接口定义 |
| 参数 | 相关参数 ID 与范围清单 |
| 纪律 | 禁止模式清单（附录 C）、受保护区域（4.3） |
| 任务 | 任务卡全文（目标/证据/验收/禁止事项） |
| 环境 | 构建与测试命令、当前分支 |

**避免**：整库投喂、无关模块源码、历史会话全文（改用交接摘要）。

### 5.2 记忆文件

| 文件 | 作用 | 维护者 |
| --- | --- | --- |
| `AGENTS.md` | 项目规则总入口：架构文档链接、编码规范、禁止模式、构建/测试命令、受保护区域、提交规范、当前里程碑 | 人类（初始），AI 只读 |
| `PROGRESS.md` | 任务看板与状态：任务 ID/状态/会话记录/证据链接/遗留问题 | AI 更新，人类核对 |
| `DECISIONS.md` | ADR 与例外批准：决策、理由、影响、日期 | 人类批准后记录 |
| `REVIEWS/` | 每个任务的审查记录与问题清单归档 | 人类 |

**`AGENTS.md` 建议内容**（M0 建立）：

```markdown
# myJV 项目规则（AI 代理必读）
1. 技术基线：Docs/myJV_开发架构文档.md（v1.4）。实现必须与之一致；歧义时停止并提问。
2. 编码规范：C++20；RT 路径无分配/锁/引用计数/IO/异常；所有常量进 Calibration.h。
3. 禁止模式：见架构文档 3.5 与本计划附录 C。
4. 受保护区域：AssetReclaimer/ToneVoice RT 路径/VoiceManager/Calibration.h/序列化/CMake/CI/依赖。
5. 构建：cmake --build build --config Release；测试：ctest 或 Catch2 可执行文件。
6. 提交：feat(<任务ID>): <摘要>；不 amend/不强推/不合并；不修改测试以通过。
7. 每个会话只做一个任务卡；结束前更新 PROGRESS.md。
```

### 5.3 会话协议

**开始**：确认任务卡 → 检查依赖已完成 → 拉取最新 develop → 复述目标与验收标准（防止理解偏差）。

**过程中**：

- 先写失败测试，再实现（测试先行）；
- 变更范围严格限定在任务卡内；
- 遇到规格歧义/受保护区域 → 停止并提问。

**结束**：

1. 运行完整门禁（构建/测试/sanitizer）；
2. 产出证据包（见 6.1）；
3. 生成会话总结（完成/未完成、证据、遗留、下一步）；
4. 更新 `PROGRESS.md`；提交 feature 分支；
5. 不合并（合并权在人类）。

### 5.4 会话交接模板

见附录 D。上下文超限或换会话时使用；交接摘要代替历史对话。

---

## 6. 质量保障

### 6.1 测试先行与证据要求

每个任务的**证据包**必须包含：

| 类型 | 证据 |
| --- | --- |
| 测试 | 新增/修改用例清单 + 运行输出摘要（通过数/失败数） |
| 静态/动态检查 | sanitizer（ASan/UBSan/RTsan）结果；编译器警告（零新增） |
| DSP | 基准音频对比（RMS/频谱）或理论曲线对比数据 |
| UI | 截图/录屏（关键交互） |
| 性能 | 前后对比（CPU/耗时/内存） |
| 参数 | 参数 ID 与范围核对表（与架构文档一致） |
| 变异抽查 | 至少 1 个用例的"破坏实现→测试变红"记录 |

**判定规则**：证据缺失 = 任务未完成；AI 自述不构成证据。

### 6.2 自动化门禁

与架构文档 10.3 一致，并补充 Vibe 专项：

| 作业 | 触发 | 内容 |
| --- | --- | --- |
| build-matrix | 每次推送 | 三平台构建 |
| unit-tests | 每次推送 | Catch2 全部用例（无跳过） |
| sanitizers | 每次推送 | ASan/UBSan；Clang RTsan |
| render-regression | 合并到 develop | 基准音频回归 |
| pluginval | 合并到 develop | level 5+ |
| soak-test | 每日 | 30 分钟压力渲染 |
| **test-integrity** | 每次推送 | 检测被跳过/禁用/删除的用例（与基线对比） |

> `test-integrity` 作业用于拦截"改测试来通过"的行为：用例数量只允许增加或经批准修改。

### 6.3 基准音频与实时安全

- 基准音频是 DSP 行为的最终事实来源；修改基准必须经人类确认并记录差异；
- RTsan/ASan 违规视为 S1，阻塞合并；
- 每个里程碑收口执行一次 30 分钟压力（频繁补丁/Zone 切换 + 64 声部）。

### 6.4 定期审计（每 2 周）

审计日（约 2–3 小时）由人类执行：

1. 全库扫描红旗模式（静态搜索 + 抽读）；
2. 抽查最近 5 个 PR 的测试有效性（变异抽查）；
3. 核对 `PROGRESS.md` 与实际仓库状态；
4. 检查架构漂移（与架构文档 diff 对照）；
5. 更新 `DECISIONS.md` 与风险登记表。

---

## 7. 纪律与护栏

### 7.1 范围纪律

- AI 只改任务卡列出的文件；发现需要任务外改动时，**提出新任务卡**而不是"顺手改"；
- 重构与功能分离：重构单独 PR，且不夹带行为变更；
- 禁止重命名公共接口/参数 ID（除非任务明确，且提供迁移）。

### 7.2 依赖与许可

- 新增第三方依赖需人类批准，并记录：用途、许可证、体积、替代方案；
- 禁止复制来源不明的代码片段；AI 生成代码在提交信息中标注任务 ID 以便追溯；
- 定期运行依赖许可扫描（M6 前完成一次全量）。

### 7.3 提交与分支

| 规则 | 说明 |
| --- | --- |
| 分支 | `feature/<任务ID>-<简述>`；不直接推 `main`/`develop` |
| 提交 | 小步提交；信息 `feat/fix/test/docs(<任务ID>): <摘要>` |
| 历史 | 不 amend、不强推、不压缩他人提交 |
| 合并 | 仅人类合并；合并前 R0 全绿 + 审查通过 |
| 测试改动 | 任何测试的删除/禁用/弱化必须在 PR 中显式说明并获批准 |

### 7.4 禁止模式（RT 路径）

```text
✗ new / delete / malloc / free
✗ std::vector / std::string / std::function 构造或扩容
✗ std::shared_ptr 拷贝或析构（含隐式）
✗ std::mutex / lock / condition_variable
✗ 文件/网络 IO、日志、异常抛出
✗ 读取 UI/模型层可变状态（必须走快照）
✗ 未定义行为的 C++ 写法（越界、悬空、未初始化）
```

---

## 8. 进度与节奏

### 8.1 看板与状态文件

- `PROGRESS.md` 为唯一事实来源，字段：任务 ID / 状态 / 会话记录 / 证据链接 / 审查级别 / 遗留问题；
- 每周五更新一次汇总（燃尽：Done/总数）。

### 8.2 每周节奏（建议）

| 日 | 内容 |
| --- | --- |
| 周一 | 规划：补全任务卡上下文，确定本周批次 |
| 周二–周四 | AI 会话 + 审查循环（每天 2–4 个任务卡） |
| 周五 | 审查清尾、审计（每两周）、更新 PROGRESS/风险 |

### 8.3 里程碑评审（R4）

每个里程碑结束时：

1. 演示可运行版本（不只看代码）；
2. 核对里程碑退出标准（原计划各节，保留）；
3. 架构一致性检查（与架构文档对照）；
4. 审查记录与证据归档；
5. 结论：放行 / 有条件放行（遗留项清单）/ 不放行。

---

## 9. 风险与对策

### 9.1 风险登记表（Vibe 特有风险优先）

| ID | 风险 | 概率 | 影响 | 触发条件 | 对策 | 责任人 |
| --- | --- | --- | --- | --- | --- | --- |
| V-01 | AI 写"假测试"（弱断言/恒真/改测试过门禁） | 高 | 高 | 变异抽查未变红；test-integrity 报警 | 测试质量审查（3.5）+ 变异抽查 + CI test-integrity | 审查者 |
| V-02 | RT 路径违规（分配/锁/引用计数） | 中 | 高 | RTsan/ASan 报警；R2 清单发现 | R2 清单 + sanitizer 门禁 + 受保护区域 | 审查者 |
| V-03 | 架构漂移（AI 按"常识"实现而非规格） | 高 | 中 | R4 一致性检查不通过 | 上下文注入章节号 + R4 + ADR | 架构负责人 |
| V-04 | 上下文丢失/会话间不一致 | 中 | 中 | 同一模块多次返工；接口冲突 | 交接模板 + PROGRESS/DECISIONS + 接口冻结 | 人类 |
| V-05 | 幻觉 API/误用现有 API | 中 | 中 | 编译通过但行为异常 | 单元测试 + 基准音频 + 审查 | 审查者 |
| V-06 | 悄悄重构/范围蔓延 | 高 | 中 | PR 出现任务外文件改动 | PR 范围检查 + 受保护区域 + 范围纪律 | 审查者 |
| V-07 | 依赖/许可风险（引入未批准库或复制代码） | 低 | 高 | 依赖清单变化；许可扫描报警 | 依赖政策 + 许可扫描 + 提交追溯 | 项目负责人 |
| V-08 | 审查疲劳（PR 过大/过密） | 中 | 中 | 审查时长失控；漏审 | PR ≤ 400 行；每日任务上限；R3 分担 | 审查者 |
| V-09 | 进度幻觉（AI 自述完成但无证据） | 中 | 中 | Done 无证据包 | 证据驱动 DoD；PROGRESS 校验 | 项目负责人 |
| V-10 | 映射常量/调音主观性导致反复 | 高 | 中 | 听感评审连续不通过 | 常量集中；分批冻结；R3 交叉评审 | 架构负责人 |
| V-11 | 单人人类审查成为瓶颈 | 中 | 中 | 待审队列 > 10 | 增加审查窗口；R3 前置过滤；压缩范围 | 项目负责人 |
| V-12 | AI 服务中断/限额 | 中 | 低 | 会话不可用 | 任务卡可交接给任意代理；文档完备 | 人类 |

### 9.2 应急裁剪

若工期或审查吞吐严重不足，按以下顺序裁剪（从低价值到高价值）：

1. 可选可视化控件（LfoScopeView、ToneLevelMeter 精细版）；
2. 采样编辑高级功能（自动映射、循环交叉淡化 UI）；
3. 撤销/重做的采样编辑部分；
4. AU 格式（先发 VST3 + Standalone；同步调整验收项 N-04）；
5. Linux 构建。

> 核心不可裁剪：Patch 参数体系、采样器核心、Structure 1–4、64 声部、实时安全、序列化、审查流程本身。

---

## 10. 验收标准

### 10.1 功能验收（v1.0）

| 编号 | 验收项 | 标准 |
| --- | --- | --- |
| F-01 | Patch Common | 架构附录 A 中 Patch Common 全部参数生效 |
| F-02 | 4 Tone 引擎 | 每 Tone 的 WG/TVF/TVA/ENV/LFO/矩阵全部参数生效 |
| F-03 | Structure 1–4 | 四种结构发声正确，Booster/RingMod 可听 |
| F-04 | 采样器 | 多 Zone、键区/力度层、循环点、反向、根音/微调正确 |
| F-05 | 声部管理 | 64 声部、Last/Loudest 抢占、Mono/Legato/Portamento 正确 |
| F-06 | Patch Remain | 切换补丁不断音、无悬空指针 |
| F-07 | MIDI | Note/CC/Bend/Aftertouch/PC/Bank Select/RPN 按规格响应 |
| F-08 | 序列化 | 补丁往返一致；宿主保存/恢复正确；旧版本可迁移 |
| F-09 | UI | 全部参数可达；预设管理可用；撤销/重做稳定 |

### 10.2 非功能验收

| 编号 | 验收项 | 标准 |
| --- | --- | --- |
| N-01 | 性能 | 64 声部满负荷 CPU < 30%（参考机 i5 级别，48 kHz/256 块） |
| N-02 | 实时安全 | RTsan/ASan 零违规；RT 路径无分配/锁/引用计数 |
| N-03 | 稳定性 | 72 小时压力测试无崩溃、无内存增长 |
| N-04 | 兼容性 | Reaper/Cubase/Studio One/Logic 矩阵测试通过 |
| N-05 | 插件验证 | pluginval level 5+（Windows/macOS） |
| N-06 | 加载性能 | 典型补丁加载 < 3 s，不阻塞音频 |
| N-07 | 安装体验 | 安装/卸载/升级正常；签名有效；无残留 |

### 10.3 过程验收（Vibe 特有）

| 编号 | 验收项 | 标准 |
| --- | --- | --- |
| P-01 | 审查覆盖 | 所有 R2 任务有审查记录；所有里程碑有 R3/R4 记录 |
| P-02 | 测试完整性 | test-integrity 无未批准用例删改；变异抽查记录完整 |
| P-03 | 追溯性 | 每个提交可追溯到任务卡与审查记录 |
| P-04 | 文档同步 | PROGRESS/DECISIONS/架构文档与代码一致 |

---

## 11. 后续版本路线图

| 版本 | 主题 | 主要内容 |
| --- | --- | --- |
| v1.1 | 采样器增强 | 磁盘流式、SFZ 导入、Round Robin/Release 采样、编辑高级功能 |
| v1.2 | 兼容与扩展 | SysEx 参数导入、Rhythm Set、多 Patch（Performance）、Control 目标扩充 |
| v1.3+ | 效果与生态 | 效果器接口实现（预留区）、MPE、微调音（SCL/KBM）、预设云同步（可选） |

---

## 附录 A：任务卡模板

```markdown
## <任务ID>：<标题>

### 目标
<一句话可验证的目标>

### 上下文
- 架构文档：第 <章节号> 节
- 相关文件/接口：<路径>
- 参数 ID：<列表>

### 交付物
- 源文件：<路径>
- 测试：<路径与用例清单>
- 文档：<更新项>

### 证据（必须附）
- [ ] 测试运行输出摘要
- [ ] sanitizer 结果
- [ ] DSP 基准/理论对比（如适用）
- [ ] UI 截图/录屏（如适用）
- [ ] 性能前后对比（如适用）
- [ ] 参数核对表（如适用）
- [ ] 变异抽查记录（至少 1 条）

### 验收标准（DoD）
1. …
2. …

### 禁止事项
- 不得修改：<受保护文件/任务外文件>
- 不得：<其他限制>

### 审查级别
R1 / R2（R3 由里程碑收口统一执行）

### 依赖
<前置任务 ID>

### 预估
<AI 会话数>
```

---

## 附录 B：任务提示词模板

```text
你是 myJV 项目的实现代理。本会话只完成一个任务卡。

【项目规则】
- 技术基线：Docs/myJV_开发架构文档.md v1.4（相关章节：<章节号>）
- RT 路径禁止：分配、锁、引用计数、IO、异常（见架构 3.5）
- 受保护区域：<如涉及，必须停止并提问>
- 提交规范：feat(<任务ID>): <摘要>；不 amend、不强推、不合并

【任务卡】
<粘贴附录 A 格式的任务卡>

【环境】
- 分支：feature/<任务ID>-<简述>
- 构建：<命令>
- 测试：<命令>
- 基准：<命令>

【工作方式】
1. 先写失败测试，再实现；
2. 变更范围严格限定在任务卡内；
3. 完成后运行全部门禁并产出证据包；
4. 生成会话总结并更新 PROGRESS.md；
5. 遇到规格歧义或需要任务外改动：停止并提问，不要自行决定。
```

---

## 附录 C：审查清单

### C.1 通用（R1）

- [ ] 变更范围与任务卡一致，无任务外文件改动；
- [ ] 提交信息含任务 ID；无 amend/强推痕迹；
- [ ] 测试新增且有效（非恒真/非实现细节）；
- [ ] 参数 ID/范围与架构文档一致；
- [ ] 无 magic number（常量进 `Calibration.h`）；
- [ ] 注释与代码行为一致；
- [ ] 无未批准依赖；
- [ ] 证据包完整。

### C.2 RT 路径（R2）

- [ ] 无分配/锁/引用计数/IO/异常；
- [ ] 不读 UI/模型可变状态（走快照）；
- [ ] 每采样恰好一次 `updateModulators`；调用顺序符合 5.3/5.10；
- [ ] 无每采样分支抖动；
- [ ] 生命周期：捕获的 Runtime/ZoneSet 指针有效（受回收规则保护）。

### C.3 DSP（R2）

- [ ] 数值边界（除零/NaN/denormal/越界）防护；
- [ ] 映射端点与单调性；
- [ ] 包络 Release 从当前值出发；重触发无跳变；
- [ ] 滤波器频响与理论对比在容差内；
- [ ] 随机源确定性种子可用；
- [ ] 基准音频差异已确认。

### C.4 UI（R1）

- [ ] UI 线程无音频线程操作；无阻塞加载；
- [ ] 参数显示与映射单一来源一致；
- [ ] 大参数集页面懒加载；
- [ ] 截图/录屏证据完整。

### C.5 序列化（R2）

- [ ] schemaVersion 与迁移表；
- [ ] 往返精度无损；
- [ ] 缺失资源降级路径；
- [ ] 宿主状态大小受控。

---

## 附录 D：会话交接模板

```markdown
## 会话交接：<任务ID>（<日期>）

### 已完成
- <文件/行为/测试>

### 证据
- 测试：<摘要与路径>
- sanitizer：<结果>
- 其他：<基准/截图>

### 未完成 / 遗留
- <事项 + 原因>

### 关键决策
- <决策 + 理由（同步 DECISIONS.md）>

### 下一步
- <下一个具体动作>

### 注意
- <坑点/易错处/受保护区域提醒>
```

---

## 附录 E：参考资料

| 资料 | 位置 |
| --- | --- |
| 架构文档（技术基线 v1.4） | `Docs/myJV_开发架构文档.md` |
| 参数语义资料 | `Docs/Roland_JV-1080_参数与工作方式.md` |
| JUCE 官方文档 | https://juce.com/learn/documentation |
| pluginval | https://github.com/Tracktion/pluginval |
| RealtimeSanitizer | https://clang.llvm.org/docs/RealtimeSanitizer.html |
| Catch2 | https://github.com/catchorg/Catch2 |
